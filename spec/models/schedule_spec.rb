# require 'spec_helper'

# describe Schedule do
#   it {should have_valid(:start).when(90, 600)}
#   it {should_not have_valid(:start).when("Five","Oh whenever, that's cool.")}

#   it {should have_valid(:duration).when(60, 900)}
#   it {should_not have_valid(:duration).when("Five","Oh whenever, that's cool.")}

# end
